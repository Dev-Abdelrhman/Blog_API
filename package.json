{
  "name": "root",
  "private": true,
  "dependencies": {
    "concurrently": "^9.0.1"
  },
  "devDependencies": {
    "lerna": "^9.0.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev_server": "lerna run start --scope=project_test",
    "dev_front": "lerna run dev --scope=client",
    "dev": "concurrently \"lerna run start --scope=project_test\"  \"lerna run dev --scope=my-app\""
  }
}
