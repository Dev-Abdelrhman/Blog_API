# Prisma Setup Tasks

- [x] Install `dotenv` package using pnpm
- [x] Edit `prisma.config.ts` to add `import "dotenv/config";` at the top
- [x] Run `prisma dev` to start the local Prisma Postgres server
- [x] Define a basic User model in `prisma/schema.prisma`
- [x] Run `prisma migrate dev` to apply migrations
- [x] Test the setup by running the NestJS app and verifying database connection
